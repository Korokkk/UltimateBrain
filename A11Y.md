set `alt` attribute to all image elements

use `aria` attributes to elements 

`Color` of all content are contrast, easy to read

Use `placeholder` for textfield when neccessary

Use proper variant header , like `h1` `Typography variant='h6'`

set `<input />` 's type attributes properly, like email input's type is set as `type='email'`

use `label` for inputs
